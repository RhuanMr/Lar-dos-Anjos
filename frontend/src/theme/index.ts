import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: { main: '#88E788' },
    secondary: { main: '#B3EBF2' },
    background: { default: '#FFFFFF' },
  },
  typography: {
    fontFamily: 'Roboto, sans-serif',
  },
});

